

## שינוי הרכב קבוצות לטורניר זוגות 6 משחקים

### המצב הנוכחי (11 קבוצות לזוג)

| קטגוריה | כמות |
|----------|------|
| Prime | 1 |
| 5★ (מועדונים/נבחרות) | 3 |
| 4.5★ (מועדונים/נבחרות) | 4 |
| 4★ | 3 |
| **סה"כ** | **11** |

### המצב החדש (11 קבוצות לזוג)

| קטגוריה | כמות | שינוי |
|----------|------|-------|
| Prime | 0 | ❌ מוסר |
| 5★ (מועדונים/נבחרות) | 3 | ללא שינוי |
| 4.5★ (מועדונים/נבחרות) | 4 | ללא שינוי |
| 4★ | 4 | +1 ✅ |
| **סה"כ** | **11** | |

---

## פירוט טכני

### קובץ: `src/services/teamSelector.ts`

**שורות 230-252** - פונקציה `generateTeamPoolsFor6Rounds`:

1. **הסרת בחירת Prime** (שורות 230-232):
   ```typescript
   // מוחקים את הקוד הזה:
   const prime = pickAndBan(pool, getPrimeTeams(), 5);
   if (prime) pool.push(prime);
   ```

2. **הגדלת כמות ה-4★ מ-3 ל-4** (שורות 248-252):
   ```typescript
   // משנים מ-3 ל-4:
   for (let i = 0; i < 4; i++) {  // היה: i < 3
     const club4 = pickAndBan(pool, getClubsOnly(4), 4);
     if (club4) pool.push(club4);
   }
   ```

---

## סיכום השינוי

שינוי קטן בקובץ אחד בלבד:
- `src/services/teamSelector.ts` - הסרת Prime והגדלת 4★

